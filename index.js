// Initialization
// Initialize Fixed Action Button
$(document).ready(function () {
    $('.fixed-action-btn').floatingActionButton({
        toolbarEnabled: true,
        hoverEnabled: false
    });
});

// Initialize tabs
$(document).ready(function () {
    $('.tabs').tabs();
});