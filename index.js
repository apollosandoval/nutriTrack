// Initialization
// Initialize Fixed Action Button
$(document).ready(function () {
    $('.fixed-action-btn').floatingActionButton({
        hoverEnabled: false
    });
});

// Initialize tabs
$(document).ready(function () {
    $('.tabs').tabs();
});

// Initialize Modal
$(document).ready(function () {
    $('.modal').modal();
});